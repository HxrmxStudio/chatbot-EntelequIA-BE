/**
 * Store info context - Structural constants only
 * 
 * Text content has been removed to eliminate duplication with prompt files.
 * Only structural data remains here.
 * All text content should come from the adapter (prompt files).
 */

// No structural constants needed for store-info context
// All content comes from prompt files via adapter
