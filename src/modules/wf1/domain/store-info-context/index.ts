export type {
  StoreInfoAiContext,
  StoreInfoQueryType,
  StoreInfoTemplates,
} from './types';
// No constants exported - all content comes from adapter templates
export { buildStoreInfoAiContext } from './format';
